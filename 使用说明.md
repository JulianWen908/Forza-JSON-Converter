# Forza 涂装 JSON 转换器（FH6 → FH5）

版本：v1.8.5

把 **Forza Horizon 6** 的涂装 JSON 转换为 **Forza Horizon 5（forza-painter）** 可导入的 JSON。

本版仅保留 FH6 → FH5 转换（FH5 → FH6 方向已移除），并新增「从 FH6 内存导出」功能。

## 一、从 FH6 内存导出当前彩绘组（新增）

在游戏运行时，点击「文件」卡片中的 **“从FH6内存导出”** 按钮：

1. 先确认 `ForzaHorizon6.exe` 正在运行，并且已经在游戏里打开一个彩绘组（任意图层数均可）。
2. 点击按钮后，先选择导出 JSON 的保存位置。
3. 程序会读取游戏内存，导出原始 FH6 JSON（完整类型码 + 内存坐标），并自动载入。
4. 载入后直接点击「开始转换」即可转成 FH5 可导入文件。

定位说明：

- “文件”卡片新增“图层数量”输入框（可选）。填写当前彩绘组的图层数可走更快的计数扫描定位。
- 留空时会先尝试签名定位，再尝试内存扫描；两者都失败时，请填写图层数量后重试。
- 定位过程会显示进度并写入 `%LOCALAPPDATA%\ForzaJSONConverter\fh6_dump_debug.log`。
- 定位需要扫描游戏内存，通常需 1~3 分钟；期间会显示“正在扫描内存区域 x/y”，请耐心等待，不要重复点击。

注意事项：

- 若无法打开游戏进程，请以 **管理员身份** 运行本工具（读取内存需要权限）。
- 若提示“图层数异常”或“未能定位彩绘组”，请确认已在游戏内打开一个彩绘组后再试。
- 该功能沿用 FH5 forza-painter 的「签名 + 指针链」定位方式，并附带泛化内存扫描兜底；若游戏版本更新导致偏移变化，需再适配。
- 定位会先尝试签名，未命中时自动进行内存扫描；扫描过程会显示进度，最长约 90 秒，超时会明确报错而不是卡死。
- 若仍失败，请查看 `%LOCALAPPDATA%\ForzaJSONConverter\fh6_dump_debug.log` 中的详细日志，便于反馈问题。

## 二、三角形大小调节

转换时三角形的长宽按比例缩放，提供四个选项（在“调节三角形的大小”卡片中选择）：

- 更小的三角形：40%
- 较小的三角形：45%
- 正常的三角形：59%（默认）
- 较大的三角形：65%

> 当彩绘纹饰导入出现异常时，可在此处调整三角形的大小并尝试重新导入。调整后可能仍需少量手工修正。

## 三、转换规则（FH6 → FH5）

- 在开头添加 FH5 画布描述元素，画布尺寸由形状外接范围自动推断。
- 按映射表转换形状类型：矩形 `1→1048677`、椭圆 `16→1048678`、三角形 `103→1048679`、软边椭圆 `228→1048804`。
- 像素坐标换算为游戏内存坐标：`[x, y, w, h, rot]` → `[x, -y, w/除数, h/除数, 360-rot, skew, mask]`；矩形除数 127、椭圆/软边椭圆 63、三角形按上表比例缩放。
- 若载入的是「FH6 内存导出」的原生 JSON，则形状类型码与内存坐标保持原样，仅插入画布元素。
- 输出文件采用与 forza-painter 原生一致的排版：首行 `{"shapes":`、每个形状一行、CRLF 行尾、末行 `]}`。

## 四、使用方法

1. 双击 `ForzaJSONConverter.exe` 启动。
2. 把 `.json` 文件拖入窗口，点击“选择文件”，或使用“从FH6内存导出”。
3. 在“调节三角形的大小”卡片中选择合适的三角形比例。
4. 点击“开始转换”，在弹出的保存窗口中输入文件名并选择保存位置。
5. 转换成功会弹窗提示；失败会弹窗报错。

> 标题栏右侧新增“刷新”按钮：若导出/转换卡住，点击“刷新”可立即恢复按钮并中断正在进行的导出。

## 五、设置菜单

右上角“设置”按钮包含：

- 语言：简体中文 / English / 日本語 / 한국어
- 主题：跟随系统 / 浅色 / 深色
- 背景图片：从本地选择，填充铺满、不拉伸，可调不透明度
- 背景不透明度：0–100%

功能卡片为半透明磨砂效果（不透明度比背景低 15%）。设置保存在 `%LOCALAPPDATA%\ForzaJSONConverter\config.json`。

## 六、命令行模式

`ForzaJSONConverter.exe --cli <源文件> <目标文件> [--lang zh|en|ja|ko] [--triangle 0.40|0.45|0.59|0.65]`

## 七、样例文件

- `asu_fh6_to_fh5.json`：`asu v1 1850.json`（1850 形状）转成 FH5 后的结果（默认三角形 59%）。

## 八、源代码

本版本同时保留了完整源代码（`src/` 目录）与 `README.md`、`requirements.txt`，可直接用于发布到 GitHub。
